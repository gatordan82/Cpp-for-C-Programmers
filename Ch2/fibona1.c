/**********************************************************************  Filename:  fibona1.c  Chapter:   2      Native Types and Statements  Compiler:  Borland C++     Version 5.01      Summer 1998  C++ for C Programmers, Edition 3     By Ira Pohl********************************************************************///Fibonacci series compatible with C//Fibonacci series compatible with Cunsigned fibonacci(unsigned n){   unsigned i, sum = 0, f0 = 0, f1 = 1;   for (i = 0; i < n - 1; ++i){      sum = f0 + f1;      f0 = f1;      f1 = sum;   }   if (n > 1)       return sum;   else       return n;}/* Print Fibonacci numbers and quotients. */#include   <stdio.h>int main(){   unsigned  n = 2;   printf("\nEnter a number greater than 1 for fibonacci: ");   scanf("%d", &n);   while (n > 1) {      printf("for %ld, the fibonacci number is %ld\n", n, fibonacci(n));      printf("\nEnter a number greater than 1 for fibonacci: ");      scanf ("%d", &n);   }}