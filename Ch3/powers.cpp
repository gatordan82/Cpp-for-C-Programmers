/**********************************************************************  Filename:  powers.cpp  Chapter:   3      Functions Pointers and Arrays  Compiler:  Borland C++     Version 5.01      Summer 1998  C++ for C Programmers, Edition 3     By Ira Pohl********************************************************************/#include <iostream>#include <assert>//using namespace std;//Echo a message//Recursive factorial functionint sqr_or_power(int n, int k = 2)   //k=2 is default{   assert(k > 1);  //note asserts are as in C   if (k == 2)      return (n * n);   else      return (sqr_or_power(n, k - 1) * n);}int main(){   int num;   do {         cout << "\nEnter number for square or power"                 " (zero or negative number quits): " << endl;         cin >> num;         cout << "\n " << num << " to the power of 2 is "              << sqr_or_power(num) << endl;         cout << "\n " << num << " to the power of 3 is "              << sqr_or_power(num, 3) << endl;         cout << "\n " << num << " to the power of 4 is "              << sqr_or_power(num, 4) << endl;   } while (num > 0);}