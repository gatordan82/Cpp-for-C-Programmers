/**********************************************************************  Filename:  sum_arr2.cpp  Chapter:   3      Functions Pointers and Arrays  Compiler:  Borland C++     Version 5.01      Summer 1998  C++ for C Programmers, Edition 3     By Ira Pohl********************************************************************/#include <iostream>//using namespace std;//Simple array processingconst int  N = 5;int main(){   int  a[N] = { 10, 20}, *p;           //get space for a[0],ссссс,a[4]   int  i, sum = 0;   sum = 0;   for (p = a; p < &a[N]; ++p)      sum += *p;   cout << "\nsum = " << sum << endl;   a[2] = sum;   sum = 0;   for (i = 0; i < N; ++i)      sum += a[i];   cout << "\nsum = " << sum << endl;   a[3] = sum;   sum = 0;   for (i = 0; i < N; ++i)      sum += *(a + i);   cout << "\nsum = " << sum << endl;   int look; cin >> look;}