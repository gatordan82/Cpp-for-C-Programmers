/**********************************************************************  Filename:  sum_arr4.cpp  Chapter:   3      Functions Pointers and Arrays  Compiler:  Borland C++     Version 5.01      Summer 1998  C++ for C Programmers, Edition 3     By Ira Pohl********************************************************************/#include <iostream>#include <vector>using namespace std;//Simple array processingconst int  N = 5;//sum written to use a.size() in place of nint sum(vector<int> a){   int  i, s = 0;   for (i = 0; i < a.size(); ++i)      s += a[i];   return s;}int main(){   vector<int>  a(N);   for (int i = 0; i < N; ++i)      a[i] = i * 10;   cout << "\nsum = " << sum(a) << " for " << a.size() << " elements" << endl;   int look; cin >> look;}